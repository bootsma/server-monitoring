# Monitoring Stack

## Requirements

- Docker
- Docker Compose
- NVIDIA Container Toolkit

## Install

git clone <repo>

cd monitoring

mkdir -p prometheus/data
mkdir -p grafana/data

docker compose up -d
